# Grand Horizon Hotel - Environment Variables
# Copy this file to .env and fill in your actual values

# ===========================================
# DATABASE CONFIGURATION (Required)
# ===========================================
# MongoDB connection string
# Get this from MongoDB Atlas: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/hotel?retryWrites=true&w=majority

# ===========================================
# SESSION CONFIGURATION (Required)
# ===========================================
# Secret key for session encryption (min 32 characters)
# Generate a random string for production
SESSION_SECRET=your-super-secret-session-key-here-minimum-32-characters

# ===========================================
# EMAIL CONFIGURATION (Required for notifications)
# ===========================================
# SMTP server settings for sending emails
# For Gmail: smtp.gmail.com, port 587
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Your email address
SMTP_USER=your-email@gmail.com

# For Gmail: Use an App Password, not your regular password
# Go to: Google Account > Security > 2-Step Verification > App passwords
SMTP_PASS=your-app-password-here

# ===========================================
# PAYMENT CONFIGURATION (Optional)
# ===========================================
# Stripe API keys for payment processing
# Get these from: https://dashboard.stripe.com/apikeys
# Use test keys (sk_test_ and pk_test_) for development

# Server-side secret key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Client-side publishable key (must start with VITE_ for frontend access)
VITE_STRIPE_PUBLIC_KEY=pk_test_your_stripe_publishable_key

# ===========================================
# APPLICATION SETTINGS (Optional)
# ===========================================
# Node environment (development, production)
NODE_ENV=development

# Server port (default: 5000)
PORT=5000
